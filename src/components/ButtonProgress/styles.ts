import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  buttonProgress: {
    left: '50%',
    marginLeft: -12,
    marginTop: -12,
    position: 'absolute',
    top: '50%',
  },
}))
